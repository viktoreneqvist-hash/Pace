# Copy this file to .local/pace.env. That local copy is ignored by Git.
#
#   mkdir -p .local
#   cp pace.env.example .local/pace.env
#   chmod 600 .local/pace.env
#
# Paste your own OpenAI API key after the equals sign. Do not commit it and do
# not share it with other Pace users.
OPENAI_API_KEY=
