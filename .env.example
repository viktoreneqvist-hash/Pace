# Garmin credentials must never be stored in this file.
# Pace requests credentials interactively with hidden password and MFA prompts.
# Session tokens will be stored locally in .local/garmin_tokens/ and are ignored by Git.

# Pace uses one fixed athlete timezone for activity calendar days.
PACE_ATHLETE_TIMEZONE=Europe/Stockholm
